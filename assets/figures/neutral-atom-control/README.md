# Neutral-atom control diagrams

These editable TikZ diagrams are the vector sources for the series. Run `make` to export
PDF, SVG, and a 300 dpi PNG fallback into `assets/img/neutral-atom-control/`.
